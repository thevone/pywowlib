from setuptools import setup, find_packages

setup(
    name='pywowlib',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here if needed
    ],
    python_requires='>=3.6',
)
